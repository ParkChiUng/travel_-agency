<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardCornerRadius="@dimen/card_radius"
    android:layout_marginHorizontal="@dimen/margin_cardview_horizontal"
    android:layout_marginVertical="@dimen/margin_cardview_vertical"
    app:cardElevation="@dimen/card_elevation"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/guide_image"
            android:scaleType="centerCrop"
            android:layout_width="@dimen/image_size_width"
            android:layout_height="@dimen/image_size_height"
            android:layout_marginTop="@dimen/margin_top_photo"
            android:src="@drawable/circle_person"

            />

        <TextView
            android:id="@+id/guide_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginVertical="@dimen/margin_guidename_vertical"
            android:textStyle="bold"
            android:textColor="@color/primary_text"
            android:textSize="@dimen/text_size_guidename"
            tools:text="@string/guide_name" />

    </LinearLayout>

</androidx.cardview.widget.CardView>